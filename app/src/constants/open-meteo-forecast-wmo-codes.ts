import codes from "./open-meteo-forecast-wmo-codes.json";

/** WMO codes returned by Open-Meteo daily forecast. Edit the JSON to add support. */
export const OPEN_METEO_FORECAST_WMO_CODES = codes as readonly number[];
